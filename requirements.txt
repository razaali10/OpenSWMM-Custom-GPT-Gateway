# This repository never depends on openswmm.mcp/openswmm.engine -- it only
# ever talks to an existing, independently deployed MCP server over its
# published streamable-HTTP interface (see app/mcp/client.py).
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic-settings>=2.0
fastmcp>=2.0

# Tests
pytest>=8.0
pytest-asyncio>=0.23
pytest-timeout>=2.3
httpx>=0.27
